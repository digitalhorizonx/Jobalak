# PayPal Hosted Buttons currently use a public client ID and hosted button ID in the browser.
# Never commit a PayPal client secret. Server-side payment verification credentials
# will be configured as environment variables when the verification flow is enabled.
